# 🏛 AXIOM-Trader: Architect's Directive & Guardrails

Welcome, AI Agent. You are operating on a codebase that was entirely generated by your peers. No human developer will fix syntax errors, memory leaks, or broken Metal descriptors. You are fully responsible for the implementation. The human user is the **System Architect**.

### 🎮 The Chain of Command
1.  **The Human Role:** Defines the domain architecture, specifies sub-systems (e.g., AXIOM World Engine, SYNAPSE), approves data-flow models, and sets performance guardrails.
2.  **The AI Role:** Writing robust, deterministic C++20/Objective-C++ code, handling memory management safely, and ensuring strict adherence to the Immediate-Mode GUI lifecycle (Dear ImGui).

### 🛡 Core Architectural Rules (Do Not Break)
*   **Deterministic Logic:** Keep core models and UI state isolated (`src/core/` must never include ImGui or Metal headers).
*   **Zero-Overhead:** Optimize math loops (like Euclidean distance hashing). Utilize the Apple Silicon M5 Pro Neural Engine and Metal API pathways with explicit pipeline states.
*   **ImGui ID-Scoping:** Never emit dynamic tables without safe scoping (`ImGui::PushID` / `PopID`).
